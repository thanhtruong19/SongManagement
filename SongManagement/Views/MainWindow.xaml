<Window x:Class="SongManagement.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SongManagement.Views"
        xmlns:vm="clr-namespace:SongManagement.ViewModel"
        mc:Ignorable="d"
        Title="Song Management" Height="600" Width="900">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Song Management" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>

            <!-- Các nút chức năng -->
            <StackPanel Orientation="Horizontal">
                <Button Content="Search" Click="SearchButton_Click" Margin="5,0"/>
                <Button Content="Add" Command="{Binding AddCommand}" Margin="5,0"/>
                <Button Content="Update" Command="{Binding UpdateCommand}" Margin="5,0"/>
                <Button Content="Delete" Command="{Binding DeleteCommand}" Margin="5,0"/>
                <Button Content="Export CSV" Command="{Binding ExportCommand}" Margin="500,0" DockPanel.Dock="Right"/>
            </StackPanel>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Songs}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="40">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="100"/>
                <DataGridTextColumn Header="Song Name" Binding="{Binding SongName}" Width="150"/>
                <DataGridTextColumn Header="Singer Name" Binding="{Binding SingerName}" Width="150"/>
                <DataGridTextColumn Header="Genre" Binding="{Binding Genre}" Width="150"/>
                <DataGridTextColumn Header="Country" Binding="{Binding Country}" Width="100"/>
                <DataGridTextColumn Header="Release Date" Binding="{Binding ReleaseDate}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
